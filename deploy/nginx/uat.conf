# Legacy host nginx config (deprecated).
#
# Shared-host UAT: add a NEW hostname only. See deploy/host-proxy/.
# Existing Docker edge must proxy to http://cohestra-uat-nginx:80, not host loopback.
# Do not steal the existing application's :80 / :443.
