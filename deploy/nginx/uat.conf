# Legacy host nginx config (deprecated).
#
# Prefer the nginx service in docker-compose.uat.yml — same routing as local dev.
# Shared config: deploy/nginx/app.conf
#
# If you still install nginx on the Ubuntu host, proxy to Docker-published ports:
#   web → 127.0.0.1:3000 (only if you re-expose web; not recommended)
#   api → 127.0.0.1:8080
#
# Recommended: expose only the Docker nginx container on :80 / :443.
