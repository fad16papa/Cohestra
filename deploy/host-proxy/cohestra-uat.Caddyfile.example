# Not used on the current UAT droplet.
# Public edge is lead-generation-crm-nginx-1 (Docker nginx), not host Caddy.
#
# Host-process Caddy (on Ubuntu, not in Docker) would use 127.0.0.1:8180.
# A Caddy container on cohestra_uat_edge would use cohestra-uat-nginx:80.
# Current droplet edge is lead-generation-crm-nginx-1 — use the nginx example.
