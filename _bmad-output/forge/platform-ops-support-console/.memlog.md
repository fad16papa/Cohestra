---
idea: Ops console v2: tenant snapshot + operator-visible reply + audited recovery + create-tenant UI + omni-search, without impersonation
goal: harden
status: complete
updated: 2026-08-18T16:49
---

- (note) Ground truth: Epic 11 directory/lifecycle, Epic 26 operator intake, Epic 27 inbox/report; FR-7 no impersonation; PlatformAdmin JWT has no tenant_id
- (note) Room: John (PM), Winston (Architect), Amelia (Dev), Sally (UX), Murat (QA); generated: Gita (locked-out operator), Kenji (tenant-isolation counsel), Nia (Gmail matcher)
- (crack) John vs Winston: changing /platform home to tickets fights FR-2 directory as the control plane. Resolve: keep directory as home; Support nav gets an open-count badge.
- (lock) Do not change default landing off Tenant directory. Badge open tickets on Support nav.
- (crack) Kenji: member emails on a snapshot look like PII export. Resolve: operator/member contact emails are already on tickets and adminContactEmail; still no client CRM dump, no registration payloads.
- (lock) Snapshot may list tenant operator/member emails + roles + lastLoginAt. Never list clients, phones, or registration answers.
- (crack) Gita: locked-out operators cannot file tickets — catch-22. Nia: a public form is an abuse hose. Resolve: this slice ships recovery for known members (audited reset/resend); public cannot-sign-in defers until rate-limited intake is designed.
- (kill) Public /support/cannot-sign-in in this slice — abuse before recovery path is proven
- (lock) Audited platform actions: SendPasswordResetEmail and ResendEmailVerification for a tenant member. Same OTP/reset pipelines as self-serve. No password set by ops. No login-as.
- (crack) Amelia: Create tenant API exists unused. Winston: provision without a membership leaves an empty husk. Resolve: UI creates tenant then shows next step (invite/complimentary); does not auto-create a TenantAdmin user.
- (lock) Create-tenant form wraps existing POST /api/v1/platform/tenants. Does not invent a user. Complimentary remains a separate audited action.
- (crack) Sally: more cards violate sparse UX-DR16. Resolve: one snapshot block reused on ticket detail and tenant detail — not a chart wall.
- (lock) One TenantSnapshot DTO/card: plan + billingStatus + isComplimentary + limit meters (used/max) + lastLoginAt + lastPublicRegistrationAt + openIssueCount + isDemoOrLoadTest + member emails. Same component on /platform/tenants/:id and /platform/support/:id.
- (crack) Nia: Gmail is the real inbox; in-app reply will be ignored. John: operators already look at Settings status. Resolve: reply lives in-app AND emails the ticket filer; Gmail matching by SUP stays.
- (lock) Operator-visible replies (not internal notes). Email the filer on reply and on status change to WaitingOnOperator / Resolved / Closed. InternalNote remains ops-only. Append-only timeline.
- (crack) Murat: omni-search across email/slug/SUP will leak cross-tenant if the query is sloppy. Winston: approved bypass only.
- (lock) Omni-search is PlatformAdminOnly and returns tenants + issues identifiers only (slug, SUP, emails). No client table scans.
- (kill) Impersonation / login-as-tenant — PRD A-5 / FR-7 / UX-DR16
- (kill) Client PII export, registration payload inspector, editing tenant activities from platform
- (kill) Stripe writes / customer-portal-as-ops; read-only billing dials already on the tenant row are enough
- (kill) Second web app, Intercom, SLA engine, ticket assignment, sponsored expiry — not this slice
- (kill) Default-hide load-test with no toggle — hide is a filter default OFF with explicit toggle
- (lock) Directory filters: billingStatus, status, hideLoadTest (default show all). Search includes operator email not only slug/name.
- (lock) Deep links both ways: issue → tenant, tenant → open issues. mailto adminContactEmail.
- (lock) Audit entries show actor email not only GUID. Recovery and replies write platform_audit_logs.
- (lock) Stay in one Next app under /platform/*. No new origin. PlatformAdminOnly on every new endpoint.
- (lock) README ops note: use a separate browser profile for /platform/login; do not tenant-login on the same apex origin.
- (decision) HARDENED: Ops console v2 = Snapshot + Reply thread + Recovery emails + Create-tenant UI + omni-search/filters + Support badge. Impersonation stays dead.
- (direction) Downstream: bmad-prd or bmad-create-epics-and-stories for Epic 28-class platform ops; then spec the snapshot DTO
