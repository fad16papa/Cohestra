---
topic: Story 24.4 — Activities list sort + URL-sync filters
goal: Define build-ready scope for Epic 24.4 after 24.1–24.3
mode: autonomous
status: complete
updated: 2026-08-09T09:42
---

- (idea) 24.1 synced status only; search/category/community still local state — breaks back button and shareable links
- (idea) Backend hard-sorts UpdatedAt desc; no sortBy API param on activities list today
- (idea) Operators at 50+ activities need Name A-Z and Registrations high-low sort
- (idea) Clients list is reference: derive filters from searchParams, sort toggle in header
- (idea) URL params: status, search, category, community, sortBy, sortDirection — page stays local like clients
- (idea) Default sort: updatedAt desc (preserve current behavior when params absent)
- (idea) Sort options v1: name, createdAt, updatedAt, registrationCount
- (idea) Search debounce writes to URL via router.replace — same as clients commitSearch
- (idea) Recovery chips (Published only, Free a slot) must compose with URL sync not fight it
- (idea) Clear filters deletes all filter params from URL; reset page to 1
- (idea) Active filter chips/banner optional v1 — URL is source of truth is enough
- (idea) Backend: ActivityService ListAsync + controller validation + unit/integration test
- (idea) Out of scope: saved views, table layout, schedule sort, EXPERIENCE.md (24.5)
- (idea) Chosen direction: mirror clients-list-page URL pattern + add sort dropdown in filter bar
- (insight) Single story: URL-sync all list filters + server-side sort with 4 fields; reuse 24.1 status sync and clients-list patterns
