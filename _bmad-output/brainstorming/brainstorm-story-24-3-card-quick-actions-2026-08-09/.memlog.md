---
topic: Story 24.3 — Activity card quick actions
goal: Copy link, registrations tab, and clients-by-activity from Activities list without opening full detail first
mode: autonomous
status: complete
updated: 2026-08-09T09:17
---

- (technique) started First Principles
- (idea) Card click = go to detail; quick actions must not hijack the whole card — separate action strip outside Link
- (idea) Copy link only meaningful for published — draft/archived show disabled + tooltip
- (idea) Registrations deep link already exists: /activities/{id}?tab=registrations
- (idea) Clients-by-activity has NO API filter today — search by name is wrong (matches client not activity)
- (idea) Reuse fetchActivityRegistrationLink + copyTextToClipboard from share kit — do not duplicate URL building
- (idea) Actions visible always on desktop — no hover-only row (EXPERIENCE anti-pattern)
- (technique) started Reverse Brainstorming
- (idea) Fail: N cards each fetch registration link on mount — 50 API calls on list load
- (idea) Fail: copy button inside Link — click navigates instead of copies
- (idea) Fail: clients link with search=activityName — returns unrelated clients named similarly
- (idea) Inverse: fetch link on Copy click only; toast Copied or error
- (idea) Inverse: add activityId query param to clients list API + web fetchClients
- (technique) started Morphological Analysis
- (idea) Action strip placement: footer above plan-reg/conflict strips
- (idea) Actions: Copy link | Registrations (count) | Clients (count) — count from activity.registrationCount for regs; clients needs API or same count as proxy
- (idea) Clients link target: /clients?activityId={id} after API added
- (idea) Icon buttons with text labels on sm+ — Link2 Users ClipboardList pattern from lucide
- (idea) Split story: 24.3a card UI + copy + regs link; 24.3b clients API filter — reject split, ship API in same story it's small
- (technique) started Cross-Pollination
- (idea) Clients row WhatsApp/Viber — text button row at bottom of card not in link zone
- (idea) Share kit copy UX — status message Select URL manually fallback
- (idea) Dashboard metric tiles — link with ariaLabel to filtered destination
- (technique) started Constraint Mapping
- (idea) ActivityCard is server component today — quick actions need client wrapper ActivityCardQuickActions or make card client
- (idea) Backend: add activityId optional filter to ClientService.ListAsync + admin clients endpoint
- (idea) Frontend: clients-list-page parse activityId from URL + pass to fetchClients
- (idea) Epic 24.3 scope includes thin API slice — ~30 lines C# + test
- (insight) Ship ActivityCardQuickActions client strip: Copy link (published, on-demand fetch), View registrations → ?tab=registrations, View clients → /clients?activityId=. Add activityId filter to clients API and list page. Place strip between card link and footer meters/conflicts. Toast on copy.
- (decision) No prefetch registration links on list load
- (decision) Include backend activityId clients filter in 24.3 — required for honest Clients action
