# Story 34.3 code review — current implementation

**Date:** 2026-09-05  
**Scope:** optional synthesis composer + guard + disabled/OpenAI synthesizer

## Layers

### Blind Hunter

- Synthesis is off unless `SynthesisEnabled` and `ApiKey` are both set.
- HTTP synthesizer is not resolved when unconfigured.
- Guard keeps evidence, hrefs, ids, kinds, priorities from facts.
- Logs use insight counts/mode only — no client names.
- API key is not logged.

### Edge Case Hunter

- Empty brief skips synthesis.
- Provider throw → deterministic fallback.
- Invented number → fallback.
- Missing insight in draft → throw → fallback.
- Timeout and max tokens are clamped.
- CI never calls a live provider.

### Acceptance Auditor

All four ACs pass via unit tests. Live provider is intentionally not required.

## Findings

### MINOR — deferred

OpenAI-compatible client is untested against a live vendor. Acceptable: fallback is the production default, and the guard is independently tested.

### NIT

Composer info log is duplicated in three branches; could be one helper.

## Severity summary

- BLOCKER: none
- MAJOR: none
- MINOR: 1 deferred (no live vendor in this environment — credentials stop condition)
- NIT: 1 remain
