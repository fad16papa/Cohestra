# Story 34.2 code review — current HEAD

**HEAD reviewed:** `6e6387cb3a97773cef60fe6a4ebf89da406fb55c`  
**Date:** 2026-09-05  
**Scope:** Dashboard “Needs attention” surface + `intelligence-api` parse

## Layers

### Blind Hunter

- Brief is fetched with the existing `authFetch` JWT helper; no new auth bypass.
- Recommended-action and evidence hrefs must be same-origin admin paths (`/` and not `//` / `://`). Unsafe action hrefs fail the whole parse (fail closed). Unsafe evidence hrefs are stripped.
- Parse rejects partial/malformed insights rather than rendering invented cards.
- Brief errors stay inline; dashboard metrics still load.

### Edge Case Hunter

- Insufficient-data message renders with no invented insights.
- Empty-activity dashboard still mounts the brief (clients-only tenants are not stuck on the empty hero).
- Loading, error+retry, and success paths are distinct.
- View modes (overview / graphs / tables) all keep the brief above the switcher.
- Evidence values such as `"0"` parse (capacity full).
- PascalCase and camelCase payloads accepted.

### Acceptance Auditor

| AC | Verdict |
| --- | --- |
| 1 Permanent surface, all views, empty-activity | PASS (browser) |
| 2 Title, why, evidence, action, safe hrefs | PASS |
| 3 Insufficient data | PASS (unit) |
| 4 Inline error, dashboard survives | PASS (code) |
| 5 Deterministic labeling | PASS (“From your workspace data”) |

## Findings

### MINOR — deferred

Clients URL still does not persist `withoutOutreach`. Same residual as 34.1. Deep link `/clients?leadStatus=new` is what the list page honors.

### NIT

Brief does not poll with dashboard metrics. Remount/refresh is enough for v1.

## Severity summary

- BLOCKER: none
- MAJOR: none
- MINOR: 1 deferred (shared with 34.1)
- NIT: 1 remain

## Verification

- `web` vitest `intelligence-api.test.ts`: 6 passed
- GitHub CI on this branch: .NET test, API integration, Next.js build, Docker smoke — pass
- Operator browser pass on seeded tenant: real names/counts, deep links to Clients filters, brief remains in graphs/tables, readable at ~390px

## Gate

No unresolved BLOCKER/MAJOR. Product/UX acceptance passed on the seeded tenant. Keep story `review` until this HEAD (or a later reviewed HEAD) is on `main`.
