# Epic 16 Retrospective — One-stop Lite (Share Kit v1)

**Date:** 2026-07-26  
**Epic:** 16 — One-stop Lite (v1.1 scope pulled forward)  
**Status:** Complete (stories 16.1–16.3)  
**PRD:** `_bmad-output/planning-artifacts/epics-cohestra-enterprise.md` (UX-DR15; originally parked post-MVP)

## Epic summary

Epic 16 delivered the first slice of One-stop Lite: operators can share activities and homepages without leaving the dashboard. The **QR & Link** tab became **Share kit** with WhatsApp copy, link preview, QR download, and share pack. Registration pages gained real Open Graph metadata for messengers and crawlers. The website builder gained homepage WhatsApp copy and a fix for publish success URLs on apex localhost admin.

Parked Epic 16 items (custom domain, thin email automations, paid tickets, seat add-ons) remain **out of scope** per PRD §13.11.

### Stories shipped

| Story | Outcome |
|-------|---------|
| 16.1 | Share kit tab replaces QR & Link; shared `ShareLinkPreview`; activity detail UI polish (Overview, Form, Registrations, Share kit) |
| 16.2 | `generateMetadata` on `/register/[slug]`; `buildActivityRegistrationMetadata`; generic fallback when not indexable |
| 16.3 | `buildHomepageWhatsAppMessage`; Copy WhatsApp in publish dialog, health strip, and success dialog |

**Delivery metrics:** 3/3 stories done · PRs #12, #13, #14 merged to `main` · 1 adversarial code review (4 patches applied) · 1 post-merge build fix (client/server door API split)

## What went well

- **Shared utilities reduced duplication** — `share-kit-utils.ts` and `ShareLinkPreview` kept activity and homepage share flows consistent (copy, preview, WhatsApp message shape).
- **UI polish bundled with feature work** — Activity detail shell cleanup (tabs, publish controls, registrations) made Share kit feel native rather than bolted on.
- **Adversarial code review caught real issues** — OG leak on draft/closed pages, Safari dual-download race, hero URL normalization, and partial-load failure UX were all fixed before merge.
- **Tenant URL resolution** — `resolvePublicSiteUrl` + door API lookup fixed the apex-localhost publish dialog showing bare `localhost:8088` instead of `creativorare.localhost:8088`.
- **Story docs stayed current** — Review findings tracked in story files with patch/defer/decision labels.

## What we learned

- **Client components must not import server modules** — `fetchPublicDoorClient` lived in `public-door-api.ts` alongside `fetchPublicDoorServer`, which pulled `next/headers` into the client bundle and broke the Next.js build. Fix: split into `public-door-client-api.ts` + shared `public-door-payload.ts`.
- **Merge conflicts on local pull often mean local drift** — Cloud fast-forward from `1c40f83` → `d72dca3` was clean; Francis's Windows conflict in `website-builder-page.tsx` came from uncommitted local edits, not upstream divergence.
- **Share pack downloads need stagger** — Browsers block simultaneous programmatic downloads; 400ms stagger in `downloadSharePackFiles` is required for QR + `.txt` pack.
- **Draft/archived share kit needs text-only unlock** — Disabled ghost buttons were confusing; text-only unlock messaging with clear publish-gate copy is the right pattern.
- **OG metadata must fail closed** — Non-indexable registration pages (draft, closed) must emit generic metadata, not rich activity OG tags.

## Review feedback patterns (across stories)

| Pattern | Stories | Resolution |
|---------|---------|------------|
| Privacy / leakage on non-public pages | 16.2 | Generic fallback metadata when not indexable |
| Browser download limitations | 16.1 | Staggered share pack downloads |
| Hero image URL shape in preview | 16.1 | `resolveHeroImageUrl` in `buildActivitySharePreview` |
| Resilient partial API failure | 16.1 | `Promise.allSettled` + retry + partial UI |
| Server/client module boundary | 16.3 (publish URL) | Split door API client module post-merge |
| Missing import after rebase | 16.3 | `resolvePublicSiteUrl` import in `website-section-fields.tsx` |

## Deferred items (documented, not blocking v1 scope)

| Item | Location | Notes |
|------|----------|-------|
| External hero URLs vs `next/image` remotePatterns | 16.1 review | Pre-existing branding constraint |
| Missing request origin for absolute `og:image` | 16.2 review | Unlikely behind nginx in production |
| Custom domain | Epic 16 parked | Pull when pilot demand |
| Thin email automations | Epic 16 parked | Pull after registration volume evidence |
| Paid tickets / seat add-ons | Epic 16 parked | v1.2+ |

## Previous retro follow-through

No Epic 15 retrospective on file. Relevant open action items from sprint-status (Epic 9 era):

| Prior action | Status |
|--------------|--------|
| Commit Epic 9 changes | ✅ Superseded — Epic 9–15 delivered and merged |
| Deploy to UAT with SitePage migrations | ⏳ Partial — local Docker UAT ongoing |
| Operator UAT checklist for website builder | ⏳ In progress — Epic 16 extends this surface |
| CI job for AdminSite/PublicSite integration tests | ❌ Not addressed — API integration tests still failing at Epic 16 merge |

## Challenges and growth areas

- **CI integration test failures** — API integration tests failed on PRs #12 and #13 at merge time; not fixed before merge. Risk of regression blind spots.
- **Local environment sync** — Operators on Windows need explicit `git reset --hard origin/main` after fast-moving merges to avoid conflict noise from local drift.
- **Sprint status lag** — `epic-16` remained `in-progress` after all stories were done until this retro.
- **Parked vs pulled scope** — Epic 16 was "parked" in planning but 16.1–16.3 were pulled forward; remaining parked items need an explicit product gate before story-breaking.

## Key insights

1. **Share kit is high perceived value, low domain risk** — Validates PRD parking rationale: safe to ship early once tenant URLs and OG metadata are correct.
2. **Reuse beats parallel panels** — Deleting `ActivityQrPanel` and `ActivityHomepageFeaturePanel` in favor of one Share kit tab reduced navigation noise.
3. **Post-merge build verification is mandatory** — Rebase/import fixes (#13) and client/server split (#14) both surfaced only at build time, not in review alone.
4. **Checkpoint UAT before declaring done** — Publish URL, WhatsApp copy, and OG unfurl need operator eyes on real tenant hostname (`creativorare.localhost:8088`).

## Significant discoveries (planning impact)

| Discovery | Recommendation |
|-----------|----------------|
| One-stop Lite v1 (share + OG + WhatsApp) is shippable without custom domain or tickets | Mark Epic 16 v1 scope **done**; keep parked table for v1.1+ |
| Client/server API colocation breaks Next.js App Router builds | Enforce separate `*-client-api.ts` modules when adding client fetches |
| CI integration tests failing on merge | Treat as critical path before next epic — do not accumulate |

**Epic update required for next work:** No — parked Epic 16 items stay parked until product pulls them forward.

## Readiness assessment

| Area | Status | Notes |
|------|--------|-------|
| Story completion | ✅ | 16.1–16.3 done; retro complete |
| Code on main | ✅ | `f45b49a` — PRs #12, #13, #14 merged |
| Next.js build | ✅ | Passes after client door API split |
| Code review | ✅ | 4/4 patches applied; decisions documented |
| CI integration tests | ⚠️ | Failed at merge; needs investigation |
| Operator UAT (local Docker) | ✅ | Signed off 2026-07-26 — publish URL, Share kit, OG, WhatsApp |
| Stakeholder acceptance | ⏳ | Pending UAT sign-off |

## Action items

| # | Action | Owner | Success criteria |
|---|--------|-------|------------------|
| 1 | Run Epic 16 UAT on local Docker: Share kit, OG unfurl on `/register/[slug]`, homepage WhatsApp + publish tenant URL | Operator | Sign-off on `creativorare.localhost:8088`; blockers filed as stories |
| 2 | Investigate and fix API integration test failures in CI | Dev | Integration test job green on PR to `main` |
| 3 | Product gate: decide whether to pull Epic 16 parked items (custom domain, thin email, tickets) or proceed to `uat-handoff-checklist` | Product | Decision recorded in sprint-status or change proposal |
| 4 | When adding client-side API fetches, use dedicated client modules — never import server-only paths from shared API files | Dev (team agreement) | No repeat of `public-door-api.ts` / `next/headers` build failure |

## Preparation for next work

**Epic 17:** Not defined.

**Likely next paths:**

- `uat-handoff-checklist` (only UAT polish item still in backlog)
- Epic 16 parked candidates (custom domain, thin email, tickets) — only after product gate
- CI hardening / integration test matrix repair

**Critical path before treating Epic 16 as operator-ready:**

1. Local Docker UAT (action item 1)
2. CI integration tests green (action item 2)

## Team agreements

- Run **checkpoint UAT** after multi-PR merges before closing an epic.
- **Split client/server API modules** at the file level when both exist in the same domain.
- **Mark epic done** in sprint-status when all stories are done, not when retro runs.
- **Parked scope stays parked** until explicit product pull-forward — no silent scope creep.

## Commits / PRs (reference)

| PR | Branch | Summary |
|----|--------|---------|
| #12 | `cursor/epic-16-activity-share-kit-4da3` | 16.1 share kit + UI polish |
| #13 | `cursor/epic-16-og-whatsapp-4da3` | 16.2 OG + 16.3 WhatsApp + publish URL |
| #14 | `cursor/fix-merge-main-conflict-4da3` | Merge resolution + client door API build fix |

---

**Retrospective facilitator:** Amelia (Developer)  
**Participants:** John (PM), Winston (Architect), Murat (Test Architect), Francis (Project Lead)
