# Story 34.1 code review — current HEAD

**HEAD reviewed:** `a207cf4cedaee6d2d8bd744b2b0d119e2437f8c5`  
**Reviewer:** BMAD code-review loop (adversarial self-pass + test evidence)  
**Date:** 2026-09-05  
**Scope:** `GET /api/v1/admin/intelligence/brief` and tests vs story 34.1 + SPEC CAP-1..5

Mandatory Code Review Loop is in force. This review is of the implementation HEAD that contains the timezone-import fix and capacity edge-case tests.

## Layers

### Blind Hunter

- TenantOperator on the controller; 401 without JWT (HTTP test).
- Fail-closed when tenant unresolved or TenantId empty.
- Explicit `TenantId == tenantId` on every query plus EF global filter.
- Evidence values are operator-visible names/counts only; no logging of PII.
- Deep links are server-composed constants or `{activity.Id:D}`, not request input.
- No LLM, no cache, no writes.

### Edge Case Hunter

- Empty tenant → insufficient data.
- Due follow-up uses `RegistrationPeriod.GetStartOfTomorrowUtc`.
- New + outreach coverage event excluded from `new_without_outreach`.
- Merge suspects isolated from tenant B.
- Capacity: remaining ≤ 3 or ≤ 15%; 0 spots; draft excluded; unlimited max skipped.
- WoW skipped when prior window < 3; emitted with both counts when ≥ 3.
- Over-registration remaining clamped to 0.
- Empty published-capped list does not query registrations.

### Acceptance Auditor

| AC | Verdict |
| --- | --- |
| 1 Contract shape | PASS |
| 2 follow_up_due + names + href | PASS |
| 3 new_without_outreach + href | PASS (see MINOR) |
| 4 merge_suspects | PASS |
| 5 capacity_pressure | PASS |
| 6 registration_wow floor | PASS |
| 7 insufficient data | PASS |
| 8 401 / fail-closed | PASS |
| 9 TenantIsolation | PASS (unit + HTTP) |

## Findings

### MINOR — deferred

**new_without_outreach deep link is wider than the insight**

The insight uses `LeadStatus.New` AND no outreach coverage events. The action href is `/clients?leadStatus=new` because the Clients list URL does not persist `withoutOutreach` today (`use-clients-list-filters.ts`). Landing still works; the list can include New people who already have outreach.

Rationale to defer: no product/architecture/security risk; 34.2 may wire `withoutOutreach` into the Clients URL if we want the exact intersection. Do not invent a dead query param.

### NIT

- Due/outreach predicates are duplicated from `ClientService` but reuse the same shared constants. Extract later if a third caller appears.

## Severity summary

- BLOCKER: none
- MAJOR: none
- MINOR: 1 deferred (documented)
- NIT: 1 remain

## Build / test evidence (this HEAD)

- `IntelligenceBriefServiceTests`: 12 passed
- `Category!=Integration`: 825 passed, 5 skipped (pre-existing Redis)
- Intelligence HTTP + isolation: 3 passed
- `Category=TenantIsolation` API: 13 passed

## Gate

No unresolved BLOCKER/MAJOR. Proceed to product acceptance. Story must not be marked `done` until acceptance + CI + final HEAD verification on the same HEAD.
