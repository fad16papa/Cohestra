# Clients bounded context

Client aggregate, E.164-normalized contact fields, lead status, and merge-suspect flag. `ClientDeduplicationService` matches by phone (priority) then email at registration ingestion (Story 3.3).
